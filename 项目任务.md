- 实现一个Java上的指针分析系统
- 提交一个自己编写的测试样例，包括代码和标准输出
- 测试用例评分规则：
- 无法在测试程序上正常运行的不合格，0分
- 如：超时（1分钟），崩溃
- 在测试程序上能输出结果，但结果不健壮(unsound)，1分
- 结果健壮，根据精度分数在1-2之间

程序样例
输入程序：
public static void main(String[] args) {
BenchmarkN.alloc(1); //标记分配点，没有标记的无需输出
A a = new A();
BenchmarkN.alloc(2);
A b = new A();
BenchmarkN.alloc(3);
A c = new A();
if (args.length>1) a=b;
BenchmarkN.test(1, a); //标记测试点编号和被测变量
BenchmarkN.test(2, c);
}
输出：
1: 1 2
2: 3
每行一个测试点，以测试点编号开头。
冒号后面是可能的分配点，多个分配点以空格分割

开发平台
- Java上常见静态分析平台（自学）：
- 太阿 Tai-e
- 部分平台已经自带指针分析，要求
    - 不能直接调用平台的指针分析模块或依赖于指针分析的功能（比如控制流分析/调用图构建）
    - 可以使用平台提供的其他支撑，比如数据流分析框架，过程内控制流图构建，Java语言化简等

测试样例Java语言限制
- 不允许Java 1.4以后的语言特性
- 包括泛型、枚举、lambda表达式等
- 不允许
- 反射
- annotation
- 额外的import语句（即JDK只能使用java.lang下面内容），除了必要的import
benchmark.internal.Benchmark和平凡的import benchmark.objects.*

- 允许
- 数组和多维数组
- 继承、重写
- 接口
- 强制类型转换
- 异常

评测机使用与代码提交
可以上传一个 .jar（使用太阿框架，用 gradle fatJar 打包）文件，会进行以下操作来运行程序：
    （如果你上传的是 .jar）
        将它放置在工作目录 /home/student/submission/submission.jar
        在工作目录下创建一个 run.sh，内容为：java -jar submission.jar -a pku-pta -cp testcase -m $1
    在工作目录目录下放置太阿所需的文件（java-benchmarks 文件夹）
    在工作目录目录下放置待分析的代码（testcase 文件夹）
    运行 ./run.sh <待测试的类名>
    读取工作目录下的输出结果（result.txt）
    将结果与标准答案对比，根据是否 Sound 及精确度打分
评测环境说明：
    评测环境安装了 OpenJDK 17 和 Python 3.10
    时间限制是每个测试用例 60s，资源限制是 --cpus 2 --pids-limit 256 --memory 6g --network none
    每组上传和评测作业的频率限制均为 60s 一次
    如有其他疑问，可以去看评测机源码（Dockerfile、main.py）

- 太阿文档较少，可以早点熟悉一下API。
- 保证sound、不崩溃以及不超时是得分高的关键
- Corner Case是更进一步的关键
- 限制使用的计算资源，并行不是可行的优化策略